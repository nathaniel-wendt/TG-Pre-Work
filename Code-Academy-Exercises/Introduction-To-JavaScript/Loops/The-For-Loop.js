// Write your code below
for (let counter = 5; counter < 11; counter++) {
  console.log(counter);
}

// A for loop contains three expressions separated by ; inside the parentheses: initialization, stopping condition, iteration statement.
