const plantNeedsWater = day => day === 'Wednesday' ? true : false;

// functions that only take one parameter don't need to be enclosed in curly braces and concise body arrow functions composed of a single-line block don't need curly braces.
